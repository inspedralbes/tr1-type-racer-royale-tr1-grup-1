SET NAMES 'utf8mb4';
CREATE TABLE WORDS (
  ID INT AUTO_INCREMENT PRIMARY KEY,
  TEXT_CONTENT VARCHAR(255),
  LANGUAGE_CODE VARCHAR(255)
);

CREATE TABLE TEXTS (
  ID INT AUTO_INCREMENT PRIMARY KEY,
  TEXT_CONTENT VARCHAR(2555),
  LANGUAGE_CODE VARCHAR(255),
  DIFFICULTY INT
);